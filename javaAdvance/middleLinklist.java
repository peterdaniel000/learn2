package javaAdvance;

public class middleLinklist {
    /*
     * class solution {
     *      public listNode middle(ListNode head){
     *          ListNode fast = head;
     *          LiatNode slow = head;
     *          while(fast != null && fast.next != null){
     *              fast = fast.next.next;
     *              slow = slow.next;
     *              }
     *          return slow;
     *      }
     * }
     */
    
}
