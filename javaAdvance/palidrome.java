package javaAdvance;

public class palidrome {
    /* private ListNode reverseList(ListNode head){
        ListNode prev = null;
        ListNode current = head;

        while (current != null){
            ListNode next = current.next;
            current.next = prev;
            prev = current;
            current = next;
           
        }
        return prev;
    }
     * public boolean palindrome(ListNode head){
     *      ListNode fast = head;
     *      ListNode slow = head;
     * 
     *      while (fast != null && fast.next != null){
     *          fast = fast.next.next;
     *          slow = slow.next;
     *          }
     *      ListNode reversed = reverseList(slow);
     *      ListNode current = head;
     *      while(reversed != null && current != null){
     *              if (reversed.val != current.val) return false;
     *              reversed = reversed.next;
     *              current = current.next;
     *          }
     *      return true;
     *    
     *      }
     */
}
