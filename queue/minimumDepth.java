package queue;

public class minimumDepth {
    /*
     *
    private int minDepthHelper(TreeNode node){
        if (node == null) return 0;
        
        if (node.left == null){
            return 1 + minDepthHelper(node.left);
        } else if (node.right == null){
            return 1 + minDepthHelper(node.right) 
        }

        return 1 + Math.min(minDepthHelper(node.right), minDepthHelper(node.left));
        }
     public int minDepth(TreeNode){
         return minDepthHelper(root);
        }
     */
}
